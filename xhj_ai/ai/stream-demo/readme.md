# 流式输出

- Agent 开发时代
  - Agent 越来越像人，走向AGI
  - 如何将工作拆分，将AI擅长的交给Agent，我们审核，不擅长的我们接管
    - 项目工程初始化交给Agent 
    1. 没有必要从0开始写vue 项目
      src/App.vue
      index.html
    2. 到github 拉取一个模板项目

- 热更新 hot reload
  开发阶段的利器  这个功能来自vite
  - 开发时，我们希望在不刷新浏览器的情况下，能够看到代码的变化
  - 这就需要使用热更新功能
文件修改->局部刷新
  以往直接刷新页面会导致丢失页面状态

vue/react 数据状态 密密麻麻

- stream 返回的就是二进制流
Unit8Array[十进制数，...] 
0-255 之间无符号整数数组
安排编码

## server 流式输出
### 后端返回的数据流
- 二进制文本
- \n 换行符 区分每个数据（data:）块 一行结束
  兼顾响应速度和传输的效率
  llm 生成token 时，返回json 格式，短一点
  一次性发送多少个 data： 的数据行，是不确定的，可能一行也可能两三行
- data： {}  json格式文本 completion 差不多的结构 
  出错 数据包有一定的大小 当json 数据超过大小，会截断
  \n 来split 然后for 循环每一行，拿到data： 后面的内容json 来做JSON.parse 是有可能失败的
  try {

  } catch (e) {
    // 出错  不能扔掉，下一段接着要 接着发送后面的部分
  }