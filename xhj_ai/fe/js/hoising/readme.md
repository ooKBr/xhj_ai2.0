# JavaScript 执行原理深入理解

## 变量提升 （Hoisting）
### 变量和函数的提升
变量和函数表达式的变量 
普通变量在编译阶段，提升变量到作用域顶部， 值是undefined
函数申明提升到作用域顶部，值是函数对象   // 因为在作用域里面函数是一等对象

## JS 是怎么运行的
### 传统
1. 代码是顺序执行的
变量提升影响了代码的可读性


### 总结
- 在执行的过程中，若使用了未声明的变量，会报错
    明显的代码错误
- 在一个变量定义之前使用它，不一定会报错，但该变量的值为undefined，而不是定义时的值
- 在一个函数定义之前使用它，不会出错，且函数执行正确
    后两个错误难理解，表明JS 代码不是一行一行执行的。
    说明代码不只有执行阶段还有编译阶段，编译阶段会为执行阶段做准备，准备好执行上下文。

## 变量提升发生在 编译阶段 （Hoisting）
- 先了解编译阶段和执行阶段
- 还有作用域
- 调用栈
- JS 是脚本语言 弱动态类型的，它没有独立的编译阶段
  就在运行前的那一刹那

## 所谓的变量提升，是指在JavaScript代码执行过程中，JavaScript引擎（Chrome V8引擎）把变量的声明部分和函数的声明部分提升到代码开头的“行为”。变量提升后，会给变量设置默认值，这个默认值就是undefined。

模拟变量提升
函数和变量在执行之前都是提升到代码开头

## JS代码的执行流程
“变量提升”意味着变量和函数的声明会在**物理层面**移动到代码的最前面。
并不准确，实际上变量和函数声明在代码的位置是不会改变的，而且是在编译阶段被JS引擎放入到内存中。

JS 代码在执行之前需要被JS 引擎编译，编译完成后，才会进入执行阶段。

## 编译阶段
词法环境的变量没有提升，在声明之前词法环境中的变量处于暂时性死区中，不能使用。
如果在声明前使用，会报错。
当代码在执行的时候，执行完了这个声明的代码，才会从暂时性死区中移除，变量就可以使用了。

输入一段代码，经过编译后，会生成两部分内容：
执行上下文对象（Execution Context）和可执行代码。
执行上下文是JS 执行一段代码的运行环境
调用一个函数，就会进入这个函数的上下文，找到上下文的变量环境、词法环境...

VariableEnvironment:
    myname -> undefined,
    showName -> function : {console.log(myname)}